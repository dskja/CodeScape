interface:
  display_name: "CodeScape"
  short_description: "Repo-specific patterns and workflows for CodeScape"
  default_prompt: "Use the CodeScape repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true